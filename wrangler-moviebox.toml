name = "moviebox-api-worker"
main = "moviebox-worker.js"
compatibility_date = "2025-01-01"
node_compat = true

# Workers configuration
workers_dev = true

# Route configuration (optional - configure after deploying to a custom domain)
# routes = [
#   { pattern = "moviebox.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
